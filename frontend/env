# Allow React Dev Server to run on any host (aaPanel / remote server friendly)
HOST=175.29.198.93



# Default React port (you can change if needed)
PORT=3000

# Prevent WebSocket connection errors when running behind reverse proxy
WDS_SOCKET_PORT=0

# (Optional) Disable strict host checking
DANGEROUSLY_DISABLE_HOST_CHECK=true